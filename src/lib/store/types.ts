export interface Job {
    title: string;
    imgUrl: string;
}

export interface SavedJob {
	title: string;
	description: string;
	url: string;
	isRemote: boolean;
	location: string;
	salary: string;
}
